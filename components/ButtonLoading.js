import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

export default function ButtonLoading() {
	return <CircularProgress style={{ color: 'white' }} size={22} />;
}
